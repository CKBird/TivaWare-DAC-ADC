TivaWare ADC/DAC is a short program that makes use of a Digital to Analog converter.
Using a function generator to create a signal that the processor reads,
it then sends it through the converter to store into an array on the processor
and finally send to an oscilloscope.